# Reboo8 Website

Reboo8 is a modern web application built with cutting-edge technologies. This repository contains the source code for the Reboo8 website.

## Running the code

Run `npm i` to install the dependencies.

Run `npm run dev` to start the development server.
